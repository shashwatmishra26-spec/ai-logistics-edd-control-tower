# Core data + ML stack (all standard, no exotic dependencies)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
openpyxl>=3.1
matplotlib>=3.7

# Testing (stdlib unittest is used by default; pytest optional if available)
pytest>=7.0 ; extra == "dev"
